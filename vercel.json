{
  "version": 2,
  "buildCommand": "npm install --legacy-peer-deps && echo 'Skipping build step, Vercel will handle this automatically'",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs",
  "env": {
    "VERCEL_FORCE_NO_BUILD_CACHE": "1"
  }
} 